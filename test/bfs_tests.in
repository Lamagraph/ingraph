use "./src/bfs.in";


ListFilterVerts(r, rxs, [(1, 2), (4, 3)]) ~ [1, 2, 3];

r rxs; free ifce;


graph ~ [(1, [2, 3]), (2, [1, 5]), (3, [1, 4, 5]), (4, [3, 5]), (5, [2, 3, 4])];
const StartVertex = 1;
bfs(r) ~ (graph, StartVertex);

msort(rsorted, %fst_comparer) ~ r;

rsorted; free ifce;
